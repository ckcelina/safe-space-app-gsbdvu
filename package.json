
{
  "name": "Natively",
  "version": "1.0.0",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "test": "jest",
    "lint": "eslint .",
    "lint:cache": "eslint . --cache",
    "lint:clean": "rm -rf node_modules/.cache/eslint .eslintcache && npm run lint",
    "lint:fix": "eslint . --fix"
  }
}
